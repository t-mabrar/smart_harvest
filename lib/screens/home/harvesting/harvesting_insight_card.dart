import 'package:flutter/material.dart';

class HarvestingInsightCard extends StatelessWidget {
  const HarvestingInsightCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
